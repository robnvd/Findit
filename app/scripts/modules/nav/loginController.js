(function() {
'use strict';

    angular
        .module('LicentaWeb.Controllers')
        .controller('loginController', controller);

    controller.$inject = ['$scope', 'auth', 'store', '$state'];
    function controller($scope, auth, store, $state) {
        
    }
})();